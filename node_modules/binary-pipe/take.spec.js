"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const bufferGenerator_1 = require("./bufferGenerator");
const take_1 = require("./take");
describe('take', () => {
    it('should take given number of bytes from generator', () => {
        const generator = bufferGenerator_1.bufferGenerator(Buffer.from([1, 2, 3, 4, 5]));
        const result = take_1.take(generator, 2);
        const arrayedResult = Array.from(result);
        expect(result).toBeInstanceOf(Buffer);
        expect(arrayedResult[0]).toBe(1);
        expect(arrayedResult[1]).toBe(2);
        expect(arrayedResult[2]).toBeUndefined();
    });
    it('should not iterate more than necessary', () => {
        const generator = bufferGenerator_1.bufferGenerator(Buffer.from([1, 2, 3, 4, 5]));
        take_1.take(generator, 2);
        const nextValue = generator.next().value;
        expect(nextValue).toBe(3);
    });
});
//# sourceMappingURL=take.spec.js.map