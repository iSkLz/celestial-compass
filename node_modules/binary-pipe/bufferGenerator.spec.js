"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const bufferGenerator_1 = require("./bufferGenerator");
describe('bufferGenerator', () => {
    it('should return complete buffer already including done', () => {
        const generator = bufferGenerator_1.bufferGenerator(Buffer.from([1, 2, 3]));
        expect(generator.next()).toEqual({ value: 1, done: false });
        expect(generator.next()).toEqual({ value: 2, done: false });
        expect(generator.next()).toEqual({ value: 3, done: true });
    });
    it('should throw for empty buffer', () => {
        expect(() => {
            bufferGenerator_1.bufferGenerator(Buffer.from([])).next();
        }).toThrow(bufferGenerator_1.EmptyIteratorBufferError);
    });
});
//# sourceMappingURL=bufferGenerator.spec.js.map